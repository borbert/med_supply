/**
 * Inventory Layout
 * 
 * Layout wrapper for inventory pages
 */

import AuthenticatedLayout from '../layout.authenticated'

export default function InventoryLayout({ children }) {
  return children
}
